<%- include('../../../partials/header.ejs') %>

  <!-- Page Wrapper -->
  <div id="wrapper">

    <!-- Sidebar -->
    <%- include('../../../partials/sidebar.ejs') %>
    <!-- End of Sidebar -->

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

      <!-- Main Content -->
      <div id="content">

        <!-- Topbar -->
        <%- include('../../../partials/navbar.ejs') %>
        <!-- End of Topbar -->

        <!-- Begin Page Content -->
        <div class="container-fluid">
          
          <!-- Alert -->
          <%- include('../../../partials/message.ejs') %>
          <!-- End Alert -->

          <!-- Heading -->
          <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
          <!-- End Page Heading -->

          <!-- Content Condition -->
            <!-- Tab Nav -->
            <ul class="nav nav-tabs" id="myTab" role="tablist">
              <li class="nav-item" role="presentation">
                <a class="nav-link active" id="home-tab" data-toggle="tab" href="#show-feature" role="tab" aria-controls="home" aria-selected="true">Show Feature</a>
              </li>
              <li class="nav-item" role="presentation">
                <a class="nav-link" id="profile-tab" data-toggle="tab" href="#show-activity" role="tab" aria-controls="profile" aria-selected="false">Show Activity</a>
              </li>
            </ul>
            <div class="tab-content" id="myTabContent">
              <%- include('show_feature.ejs') %>
              <%- include('show_activity.ejs') %>
            </div>
            <!-- End Tab Nav -->
          <!-- End Content Condition -->
          



        </div>
        <!-- /.container-fluid -->

      </div>
      <!-- End of Main Content -->

      <!-- Footer -->
      <%- include('../../../partials/footer.ejs') %>
      <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

  </div>
  <!-- End of Page Wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>

  <!-- Logout Modal-->
  <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
          <button class="close" type="button" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
        </div>
        <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
        <div class="modal-footer">
          <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
          <a class="btn btn-primary" href="login.html">Logout</a>
        </div>
      </div>
    </div>
  </div>

<%- include('show_modal_activity.ejs') %>
<%- include('show_modal_feature.ejs') %>
<%- include('../../../partials/js.ejs') %>
<script>
  $(document).ready(function(){
    $('#dataTableActivity').DataTable();
  })
  $('#dataTableActivity').on('click', '.button-update', function(){
    let id = $(this).data('id') /* data from attribute data-id from <a> tag */
    let name = $(this).data('name') /* data from attribute data-id from <a> tag */
    let type = $(this).data('type') /* data from attribute data-id from <a> tag */
    $('#edit-modal-activity').modal('show') /* show modal */
    $('.id').val(id) /* send data to attribute value in <input> tag with class .id as intermediary */
    $('.name').val(name) /* send data to attribute value in <input> tag with class .name as intermediary */
    $('.type').val(type) /* send data to attribute value in <input> tag with class .type as intermediary */
  })

  CKEDITOR.replace( 'about' );
  
  /* show modal edit */
  $('#dataTable').on('click', '.button-update', function(){
    let id = $(this).data('id') /* data from attribute data-id from <a> tag */
    let name = $(this).data('name') /* data from attribute data-id from <a> tag */
    let qty = $(this).data('qty') /* data from attribute data-id from <a> tag */
    $('#edit-modal-feature').modal('show') /* show modal */
    $('.id').val(id) /* send data to attribute value in <input> tag with class .id as intermediary */
    $('.name').val(name) /* send data to attribute value in <input> tag with class .name as intermediary */
    $('.qty').val(qty) /* send data to attribute value in <input> tag with class .qty as intermediary */
  })
  /* End show modal edit */
</script>